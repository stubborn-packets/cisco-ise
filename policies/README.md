# policies

Policy objects as **data** (YAML), not hand-written Terraform — this is the
directory you'll actually edit day to day once the project is up and
running, per `docs/adr/0002-tech-stack-selection.md`.

Nothing here yet. First populated in Phase 5, starting with a single,
lowest-risk object type in the lab environment.
